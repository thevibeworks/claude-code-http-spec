  return A;
}
async function rlQ() {
  try {
    if (b1().claudeCodeFirstTokenDate !== void 0) return;
    let Q = aY();
    if (Q.error) {
      t(Error(`Failed to get auth headers: ${Q.error}`));
      return;
    }
    let G = `${D9().BASE_API_URL}/api/organization/claude_code_first_token_date`,
      Y =
        (await SQ.get(G, { headers: { ...Q.headers, "User-Agent": ZW() } }))
          .data?.first_token_date ?? null;
    if (Y !== null) {
      let J = new Date(Y).getTime();
      if (isNaN(J)) {
        t(Error(`Received invalid first_token_date from API: ${Y}`));
        return;
      }
    }
    d0((J) => ({ ...J, claudeCodeFirstTokenDate: Y }));
  } catch (A) {
    t(A instanceof Error ? A : Error(String(A)));
  }
}
function zqA() {
  if (Pg1()) return "Sonnet 4.5 with 1M context · Best for everyday tasks";
  if (CqA() || $qA()) return "Opus 4.5 · Most capable for complex work";
  if (NrA() && wZA()) return "Opus 4.5 · Most capable for complex work";
  return "Sonnet 4.5 · Best for everyday tasks";
}
function kD8(A) {
  if (A === "opusplan") return "Opus 4.5 in plan mode, else Sonnet 4.5";
  return wC($K(A));
}
function slQ(A) {
  if (A === "opusplan") return "Opus Plan";
